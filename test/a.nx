void main () {
    $println ("test");
    printStr ("le test");
    printInt (1 + 2);
    printAdd (4, 4);
}

void printStr (string str) {
    $println (str);
}

void printInt (int a) {
    $println (a);
}

void printAdd (int c, int d) {
    printInt (c + d);
}

/*
faild because of the types
void printAdd (int a, int b) {
printInt (a + b);
}
 */
