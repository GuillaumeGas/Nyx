import std.Syscalls;

void main () {

    print ("More or Less !");
    int random = $rand () % 100;
    int rep = 0;
    while (rep != random) {
	res = $getInt ();
	if (res < random) {
	    $print ("C'est plus !");
	} else if (res > random) {
	    $print ("C'est moins !");
	}
    }
    $print ("Bravo !");

    return 0;
}

int test (int a, Integer b) {
    while:test (test) {
	    foo ([1, b.intValue (), 3]);
	    test ([1 .. 2]);
	    if (a) {
		for (i in [foo (.2) .. 3+5]) {
		    print (fo(2.3), 5+i);
		}
		break test;
	    } else {
		for:ff (a in [2, 5]) {
			print (a);
		    }
		return !a;
	    }
	}
    return 2*(2+3);
}
